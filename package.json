{
  "name": "react-to-figma",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/react-to-figma",
    "packages/figma-plugin"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build -w packages/shared",
    "build:react-to-figma": "npm run build -w packages/react-to-figma",
    "build:plugin": "npm run build -w packages/figma-plugin",
    "extract-keys": "npx tsx scripts/extract-figma-keys.ts",
    "version:patch": "npm version patch --no-git-tag-version --workspaces && npm version patch --no-git-tag-version",
    "version:minor": "npm version minor --no-git-tag-version --workspaces && npm version minor --no-git-tag-version",
    "version:major": "npm version major --no-git-tag-version --workspaces && npm version major --no-git-tag-version",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
